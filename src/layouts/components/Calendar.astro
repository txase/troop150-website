This component exists in order to render the FullCalendar React component on the
client side. This is what the client:only attribute achieves, which is only
available within .astro and .mdx files.
---
import FullCalendar from '@/components/FullCalendar'
---
<FullCalendar client:only="react" />
